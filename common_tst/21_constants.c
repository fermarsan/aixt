// Aixt project (C generated code)
// Device = x64
// Board = PC

#include <stdbool.h>


int main() {
	long x = 0;
	for(int i=0; i<main.repetitions; i++) {
		x += (main.e * main.e) + (main.pi / 2);
	}
	return 0;
}