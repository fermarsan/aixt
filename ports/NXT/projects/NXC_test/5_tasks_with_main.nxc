// This NXC code was automatically generated by Aixt Project
// Device = Atmel AT91SAM7S256 + Atmel AVR ATmega48
// Board = LEGO Mindstorms NXT intelligent brick
// Backend = nxc



___includes_block___

task  move_square(while(TRUE) {
Acquire(move_mutex);
OnFwd(OUT_AC, 75);
Wait(1000);
OnRev(OUT_C, 75);
Wait(500);
Release(move_mutex);
}
}

task  check_sensors(while(TRUE) {
if(SENSOR_1 == 1) {
Acquire(move_mutex);
OnRev(OUT_AC, 75);
Wait(500);
OnFwd(OUT_A, 75);
Wait(500);
Release(move_mutex);
}
;
}
}

task main() {
 move_mutex = mutex("");
Precedes(move_square, check_sensors);
SetSensorTouch(in_1);
}